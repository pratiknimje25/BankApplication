spring:
  application:
    name: BANKING-APPLICATION

#Database configuration
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bankingDB?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

#mail configuration

  mail:
    host: smtp.gmail.com
    port: 587
    username: pnimje25@gmail.com
    password: uuyc weva qupx akkm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

